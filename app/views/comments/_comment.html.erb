 <% comments.each do |comment| %>
    <p><%= comment.body %></p>
    <span class="timestamp">
        <%= t("post_ago", time: time_ago_in_words(comment.created_at)) %>
         <%= button_to t("Delete"), comment, method: :delete %>
    </span>
<% end %>
